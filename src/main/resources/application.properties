spring.application.name=ai-recruiter

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://6jb2zpuk.us-east.database.insforge.app:5432/insforge?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:7f6d485e080c89513bd3ac9adaecf7bc}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security / JWT
jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
jwt.expiration=86400000

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# AI Engine Connection
ai.engine.url=${AI_ENGINE_URL:http://localhost:8000}

# Mail Configuration
spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:25}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:false}
# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SPubnrq4KWJXsN}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:PHyZy682h6dVh4DTYDbFYJHU}
